class Triangle

  def initialize(rows)
    @rows = []
  end

  def rows
    #[[], [], []]
    # for each row look at the last row (current row index -1) and
    # the number on either side of the current indev (current 
    # number = previous rows (current index -1 + current index +1 )
  end

end